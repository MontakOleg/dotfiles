if [[ -x /opt/homebrew/bin/brew ]]; then
  eval "$(/opt/homebrew/bin/brew shellenv --shell=zsh)"
fi

# /etc/zprofile and Homebrew reorder PATH after .zshenv. Restore mise's priority.
source "$HOME/dotfiles/zsh/env"
